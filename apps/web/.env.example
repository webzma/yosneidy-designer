# Proyecto Sanity del que lee el sitio (manage.sanity.io). Obligatorio: todo
# el contenido mostrado viene de Sanity (sembrar con `pnpm --filter studio seed`).
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
# URL pública del sitio, sin barra final. En producción: https://tu-dominio.com.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
